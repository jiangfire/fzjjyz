# 架构文档

本文档描述 fzjjyz 的系统架构、模块设计和实现细节。

## 🏗️ 系统概览

```
┌─────────────────────────────────────────────────────────────┐
│                        CLI 层 (cmd/fzjjyz)                   │
│  encrypt | decrypt | keygen | keymanage | info | version    │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                    核心层 (internal/crypto)                  │
│  operations | keyfile | keygen | hybrid | signature         │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                    格式层 (internal/format)                  │
│  header | parser | serialization                            │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                    工具层 (internal/utils)                   │
│  errors | logger | buffer_pool                              │
└─────────────────────────────────────────────────────────────┘
```

## 📦 模块详解

### 1. 密码学核心 (internal/crypto)

#### hybrid.go - 混合加密系统

**职责**: Kyber768 + ECDH 双重密钥封装

```go
type HybridEncryptor struct {
    kyberPub kem.PublicKey
    ecdhPub  *ecdh.PublicKey
}

func (e *HybridEncryptor) Encapsulate() (
    encapsulated []byte,  // Kyber 密文
    ecdhPub []byte,       // 临时 ECDH 公钥
    sharedSecret []byte,  // 组合共享密钥 (32字节)
    error
)
```

**密钥派生流程**:
```
Kyber 密文 + ECDH 公钥
    ↓
Kyber 解封装 → Kyber 共享密钥 (32字节)
ECDH 密钥交换 → ECDH 共享密钥 (32字节)
    ↓
SHA256(Kyber密钥 || ECDH密钥) → 最终密钥 (32字节)
```

#### signature.go - 签名系统

**职责**: Dilithium3 签名和验证

```go
func SignHash(hash []byte, priv interface{}) ([]byte, error)
func VerifyHashSignature(hash []byte, sig []byte, pub interface{}) (bool, error)
```

**特点**:
- 支持可选签名（nil 表示跳过）
- Dilithium3 模式 3 (安全级别 3)

#### operations_shared.go - 共享操作库

**职责**: 消除代码重复的核心函数

**提取的函数**:
- `prepareEncryptionKeys()` - 密钥准备
- `encryptAESGCM()` / `decryptAESGCM()` - AES 加解密
- `calculateHash()` - SHA256 哈希
- `signHash()` / `verifyHashSignature()` - 签名
- `buildFileHeader()` - 构建文件头
- `writeEncryptedFile()` / `parseEncryptedFile()` - 文件 I/O
- `EncryptFileCore()` - 核心加密逻辑
- `DecryptFileCore()` - 核心解密逻辑

#### operations.go - 标准文件操作

**职责**: 提供简洁的加密/解密接口

```go
func EncryptFile(inputPath, outputPath string, kyberPub, ecdhPub, dilithiumPriv) error
func DecryptFile(inputPath, outputPath string, kyberPriv, ecdhPriv, dilithiumPub) error
```

**调用流程**:
```
EncryptFile
  ↓
EncryptFileCore (共享)
  ├── 读取文件
  ├── 密钥封装
  ├── AES-GCM 加密
  ├── 计算哈希
  ├── 签名
  └── 构建头部
  ↓
serializeHeader
  ↓
writeEncryptedFile
```

#### operations_stream.go - 流式操作接口

**职责**: 提供流式处理的统一接口

```go
func EncryptFileStreaming(...) error
func DecryptFileStreaming(...) error
func EncryptFileStreamingAuto(...) error  // 自动缓冲区
func DecryptFileStreamingAuto(...) error
```

**特点**:
- 调用相同的 `EncryptFileCore`
- 提供缓冲区大小控制
- 自动选择最优缓冲区

#### stream_encrypt.go / stream_decrypt.go - 流式处理器

**职责**: 封装流式处理逻辑

**重要说明**:
```
⚠️ 伪流式实现
由于 AES-GCM 需要完整数据生成认证标签，
当前实现仍需读取整个文件到内存。

真正的流式需要使用 AES-CTR + HMAC
```

**缓冲区池优化**:
```go
type BufferPool struct {
    pool sync.Pool
    size int
}
```

#### keyfile.go - 密钥管理

**职责**: 密钥文件的读写和缓存

**缓存机制**:
```go
type keyCacheEntry struct {
    key       interface{}
    createdAt time.Time
    ttl       time.Duration
}

const (
    MaxCacheSize = 100          // 最大缓存数
    DefaultCacheTTL = 1 * time.Hour
    CacheCleanupInterval = 5 * time.Minute
)
```

**安全特性**:
- 私钥文件权限检查 (0600)
- TTL 自动过期
- 大小限制防止内存泄漏
- 后台自动清理

#### keygen.go - 密钥生成

**职责**: 生成各种密钥对

**并行生成**:
```go
func GenerateKeyPairParallel() (
    kyberPub, kyberPriv,
    ecdhPub, ecdhPriv,
    dilithiumPub, dilithiumPriv,
    error
)
```

**流程**:
```
启动 3 个 goroutine
  ├── Kyber 生成
  ├── ECDH 生成
  └── Dilithium 生成
  ↓
等待所有完成 (sync.WaitGroup)
  ↓
返回结果
```

### 2. 文件格式 (internal/format)

#### header.go - 文件头结构

**格式定义**:
```
文件头 (约 4.5KB)
├── 版本 (1字节)
├── 文件名长度 (1字节)
├── 文件名 (变长)
├── 文件大小 (8字节)
├── Kyber 密文长度 (2字节)
├── Kyber 密文 (1088字节)
├── ECDH 公钥 (32字节)
├── IV (12字节)
├── 签名长度 (2字节)
├── 签名 (2700字节)
├── SHA256 哈希 (32字节)
└── 时间戳 (8字节)
```

**序列化优化**:
- 标准方法: 使用 binary.Write
- 优化方法: 手动字节操作
- 性能提升: 5x

#### parser.go - 解析器

**职责**: 从字节流解析文件头

```go
func ParseFileHeaderFromBytes(data []byte) (*FileHeader, error)
func ParseFileHeader(reader io.Reader) (*FileHeader, error)
```

### 3. 工具层 (internal/utils)

#### errors.go - 错误系统

**错误码定义**:
```go
const (
    ErrInvalidParameter = 1001
    ErrInvalidKey       = 1002
    ErrInvalidFormat    = 1003
    ErrIOError          = 2001
    ErrEncryptionFailed = 3001
    ErrDecryptionFailed = 3002
    ErrHashMismatch     = 4001
    ErrVerificationFailed = 4002
    ErrSigningFailed    = 4003
    ErrKeyGenerationFailed = 5001
    ErrSerializationFailed = 5002
    ErrAuthFailed       = 6001
)
```

#### buffer_pool.go - 缓冲区池

**职责**: 优化内存分配

```go
type BufferPool struct {
    pool sync.Pool
    size int
}

func NewBufferPool(size int) *BufferPool
func (p *BufferPool) Get() []byte
func (p *BufferPool) Put(b []byte)
```

## 🔐 安全架构

### 1. 密钥安全

```
密钥生成
  ↓
内存中使用
  ↓
加密/解密完成
  ↓
零化敏感数据 (计划中)
  ↓
返回结果
```

**当前实现**:
- ✅ 私钥文件 0600 权限
- ✅ 密钥缓存 TTL
- ✅ 缓存大小限制
- 🔄 内存零化 (待实现)

### 2. 数据完整性

```
加密流程:
  1. 读取明文
  2. 计算 SHA256(明文)
  3. 加密数据
  4. Dilithium 签名(哈希)
  5. 写入: 头部 + 密文

解密流程:
  1. 读取头部
  2. 验证头部格式
  3. 解密数据
  4. 计算 SHA256(明文)
  5. 验证哈希匹配
  6. 验证签名 (如果提供)
```

### 3. 认证加密

```
AES-256-GCM
├── 机密性: AES-CTR 模式
├── 完整性: GMAC 认证标签
└── 随机性: 12字节随机 Nonce
```

## 🔄 数据流

### 加密流程

```
用户输入: plaintext.txt
  ↓
CLI: encrypt 命令
  ↓
LoadPublicKeyCached (缓存)
  ↓
LoadDilithiumPrivateKeyCached (缓存)
  ↓
EncryptFileStreamingAuto
  ↓
EncryptFileCore
  ├── 读取文件 (10MB)
  ├── 密钥封装 (Kyber + ECDH)
  ├── AES-GCM 加密 (10MB → 10MB)
  ├── SHA256 哈希 (10MB)
  ├── Dilithium 签名
  ├── 构建头部 (4.5KB)
  └── 写入文件
  ↓
输出: plaintext.fzj (10MB + 4.5KB)
```

### 解密流程

```
用户输入: plaintext.fzj
  ↓
CLI: decrypt 命令
  ↓
读取文件头 (4.5KB)
  ↓
LoadPrivateKeyCached (缓存)
  ↓
LoadDilithiumPublicKeyCached (缓存)
  ↓
DecryptFileCore
  ├── 解析头部
  ├── 密钥解封装
  ├── AES-GCM 解密
  ├── 验证哈希
  ├── 验证签名
  └── 写入明文
  ↓
输出: plaintext.txt
```

## 📊 性能优化点

### 1. 缓存优化

**问题**: 频繁读取磁盘

**解决**: 内存缓存 + TTL

**效果**: 1000x 加速

### 2. 并行优化

**问题**: 密钥生成串行

**解决**: 3 个 goroutine 并行

**效果**: 2-3x 加速

### 3. 序列化优化

**问题**: binary.Write 性能差

**解决**: 手动字节操作

**效果**: 5x 加速

### 4. 缓冲区池

**问题**: 频繁分配/释放内存

**解决**: sync.Pool 重用

**效果**: 减少 GC 压力

## 🎯 设计决策

### 为什么使用 AES-GCM?

**优点**:
- ✅ 标准化 (NIST)
- ✅ 认证加密 (AEAD)
- ✅ 硬件加速支持
- ✅ 性能优秀

**缺点**:
- ❌ 需要完整数据
- ❌ 无法真正流式

**替代方案** (未来):
- AES-CTR + HMAC-SHA256
- 支持真正的流式处理

### 为什么使用混合加密?

**原因**:
1. **后量子安全**: Kyber 抵抗量子攻击
2. **传统安全**: ECDH 成熟可靠
3. **双重保险**: 两者同时被破解概率极低
4. **性能平衡**: Kyber 密文较大但解密快

### 为什么需要 Dilithium 签名?

**原因**:
1. **来源认证**: 确认文件创建者
2. **完整性**: 防止篡改
3. **不可否认**: 发送者无法否认
4. **后量子安全**: 抵抗量子攻击

## 🔮 未来架构

### v0.2.0: 硬件加速

```
┌─────────────────────────────────┐
│  CPU 特性检测                   │
│  (AES-NI, SHA-NI)              │
└─────────────────────────────────┘
         ↓
┌─────────────────────────────────┐
│  自动选择最优路径               │
│  - 硬件加速                    │
│  - 软件实现                    │
└─────────────────────────────────┘
```

### v0.3.0: 真正流式

```
┌─────────────────────────────────┐
│  AES-CTR + HMAC-SHA256         │
│  - 块级加密                    │
│  - 并行 HMAC                   │
│  - O(缓冲区) 内存              │
└─────────────────────────────────┘
```

### v0.4.0: 零拷贝

```
┌─────────────────────────────────┐
│  io.CopyBuffer                 │
│  - 直接写入输出                │
│  - 减少中间缓冲区              │
│  - 内存映射文件 (大文件)       │
└─────────────────────────────────┘
```

## 🧪 测试架构

### 单元测试

```
internal/crypto/
├── hybrid_test.go          # 混合加密测试
├── signature_test.go       # 签名测试
├── operations_test.go      # 文件操作测试
├── stream_test.go          # 流式测试
└── benchmark_test.go       # 基准测试
```

### 集成测试

```
cmd/fzjjyz/
└── main_test.go            # CLI 集成测试
```

### 性能测试

```bash
go test -bench=. -benchmem ./internal/crypto/
```

## 📦 依赖关系

```
fzjjyz
├── Go 标准库
│   ├── crypto/ecdh
│   ├── crypto/sha256
│   ├── encoding/pem
│   ├── io
│   ├── sync
│   └── time
│
├── Cloudflare CIRCL
│   ├── github.com/cloudflare/circl/kem
│   └── github.com/cloudflare/circl/sign/dilithium/mode3
│
└── Cobra CLI
    └── github.com/spf13/cobra
```

## 🎓 设计模式

### 1. 工厂模式

```go
func NewStreamingEncryptor(...) (*StreamingEncryptor, error)
func NewHybridEncryptor(...) *HybridEncryptor
```

### 2. 策略模式

```go
// 标准 vs 流式
EncryptFile(...)          // 标准
EncryptFileStreaming(...) // 流式
```

### 3. 池模式

```go
type BufferPool struct {
    pool sync.Pool
}
```

### 4. 单例模式 (缓存)

```go
var keyCache sync.Map  // 全局共享缓存
```

## 🔍 调试和监控

### 日志

```go
// internal/utils/logger.go (计划中)
Logger.Debug("加密开始")
Logger.Info("密钥加载完成")
Logger.Warn("缓存接近上限")
Logger.Error("加密失败", err)
```

### 指标

```go
// 计划添加
type Metrics struct {
    EncryptionTime    time.Duration
    DecryptionTime    time.Duration
    CacheHitRate      float64
    MemoryUsage       uint64
    FilesProcessed    uint64
}
```

---

**文档版本**: 1.0
**最后更新**: 2025-12-26
**维护者**: fzjjyz 开发团队
